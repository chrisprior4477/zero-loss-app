# Zero Loss project continuity checkpoint — 2026-09-05

- Project folder: `C:\Users\Win11Blue\Desktop\zero-loss-app`
- Working branch: `openai-homepage-experiment`
- Remote: `https://github.com/chrisprior4477/zero-loss-app.git`
- Local preview: `http://localhost:3000/`

## Current homepage state

The current homepage includes the polished hero and navigation, the How It Works presentation, product/reward discovery carousels, Browse the Marketplace, the interactive Dollar Wall bundle-builder, Shop by Price, the compact Someone Just Won rotator, the auto-cycling Social Activity Feed, the transparency/trust band, and the full footer.

The social feed cycles through X, Facebook, Instagram, and TikTok demo feeds every 20 seconds and uses repeated content to remain visually filled on wide screens. The winner panel auto-rotates with a cylinder-style transition.

## Development data

Homepage demonstration content is centralized in `src/lib/home/demo-data.ts`. It must be replaced with verified production integrations and fulfillment data before launch.

## Resume checklist

1. Open this project folder.
2. Confirm the branch is `openai-homepage-experiment`.
3. Run `npm install` only if dependencies are absent or the project is on a new computer.
4. Run `npm run dev`.
5. Open `http://localhost:3000/`.
