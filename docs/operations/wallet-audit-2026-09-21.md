# Wallet integrity audit — working implementation log

This is an ongoing demo-system audit, not a production-readiness sign-off.

## Owner decisions

- Preserve all sample content and existing financial history. Do not touch How It Works.
- Preserve the sample crowd while ordinary offering purchases decrease displayed availability and use the same database limit.
- Exception explicitly confirmed September 21: the Samsung TV remains a repeatable winning test case; Nike shoes and the baby bundle remain repeatable non-selected/completion-purchase test cases. Do not retire these examples, create new rounds, or branch the database to accomplish this.
- Only the owner may approve refunds or account-value adjustments. Future support staff can investigate and reply but cannot approve value changes. No new privileged account grants have been made.
- For gifting, select the intended recipient before issuance; original winner stays the winner of record. After issuance, do not promise customer self-service ownership transfer or safe code reassignment. Support review is not a promise that the provider can revoke/reissue a code.
- Owner clarified: deposited funds are not ordinarily withdrawable; exceptional refunds require review, without limiting rights for unauthorized charges or payment errors. Accidental entries should get a 30-second Undo Entry window. Deposit confirmation was approved, using fresh account authentication; this does not prove card ownership or replace issuer authentication. The Undo Entry workflow remains a separate implementation.

## Verified defects and repairs

1. Different customers could simultaneously purchase the last slot. Reproduced locally twice (two saved entries for capacity one). Migration `20260921160000` serializes the offering check; five adversarial concurrency checks now pass. Applied to hosted development-test; 30 existing lifecycle assertions passed in a rollback-only test.
2. A successful multi-entry purchase linked only by product slug and displayed “That activity is not available in your account.” Migration `20260921161500` adds exact entry/reward destinations to the existing batch receipt, including stable destinations on retries. Applied to the hosted demo after 34 rollback-only lifecycle assertions passed. Application links select the exact saved record; old responses safely fall back to the list, not an ambiguous detail.
3. Migration `20260921163000` stores the existing sample baseline and derives ordinary availability from baseline plus saved purchases, enforced under the same exclusive database lock. Applied to hosted demo after 27 rollback-only assertions passed. Item, browse and homepage rails now consume its public aggregate-only snapshot. The three allowlisted repeatable showcase scenarios preserve their sample display and outcomes. Prior history is untouched; ordinary legacy overfilled samples (Dunkin) display full and cannot accept more purchases. Full offerings remain visible after available offerings. A product with unavailable inventory data offers a refresh action instead of submitting against fallback samples.

## Verification checkpoint

- 323 application tests pass; changed application files pass ESLint.
- 257 local database assertions pass, including funding/card storage, wallet isolation, entry lifecycle, completion purchases/reward lifecycle and new availability checks.
- Five separate simultaneous-request tests pass, including two customers competing for one slot after a sample crowd of nine in a capacity-ten pool.
- Production build passes. One pre-existing invalid Testing Library option was removed from a signup navigation test so TypeScript can complete; no signup behavior changed.
- Hosted audit after live verification: 25 saved entries, no entry/debit mismatch, no batch-count mismatch, no funding/credit mismatch, no negative playable wallet.
- Local browser verified Walmart $25 initially showed four remaining after four saved entries, and the three showcase cards still showed one sample slot. Phone-width homepage checked at 390px without page overflow.
- Repair commit `0ee5116` deployed successfully to `https://zero-loss-5v47t2a14-zero-loss.vercel.app`. Verified the build log names that exact commit and `openai-homepage-experiment`. Vercel did not automatically move the existing branch alias; explicitly moved only `zero-loss-app-git-openai-homepage-experiment-zero-loss.vercel.app` to this ready deployment. Main and production aliases were untouched.
- On that live branch alias, a two-entry Walmart purchase opened the exact saved entry details (`ent_0eced5048cdf4960aa012b2ec51487d6`) successfully; closing the panel returned to six active entries. Returning to the product showed two remaining, down from four. The hosted aggregate and ledger reconciliation matched. No unavailable-activity message appeared for the new purchase.

## Test effects in the owner's demo account

- Added $1 simulated funds (not a real card charge).
- Submitted one $1 Walmart $25 gift-card entry, then a three-entry $3 batch, then a two-entry $2 batch after deployment; all private to the account.
- Starting balance $100; added $1 simulated funds and spent $6 on six demo entries. Verified resulting balance $95 and six saved active entries.
- Saved the extra-entry explanation acknowledgment after testing its checkbox and save flow.
- No invitations, real rewards, real payments, or external customer messages were sent.

## Remaining work / release gates

- Repeat/reload handling is implemented and verified in the durable recovery checkpoint below. Continue provider-specific unknown-outcome testing before real-money release.
- Durable customer Support intake/conversations are implemented and verified below. The owner must designate the reviewer login before the private inbox is enabled. Value approvals remain a separate, unimplemented permission/workflow.
- Refund lifecycle, provider exceptions, disputes and financial corrections need durable reason/actor/original-transaction linkage. Demo funding currently exercises success and replay, not a complete external-provider refund lifecycle.
- Gifting requires an auditable recipient designation and issuance lock. Provider-specific eligibility, cancellation and credential handling must be confirmed before enabling real fulfillment.
- No merge to real-money mode or claim that switching off demo mode alone makes the system launch-ready.

## Deposit confirmation checkpoint — September 21

- Main-site-styled, same-page confirmation dialog shows exact amount, sample card, funds-use disclosure, password field and explicit acknowledgment. Cancel/Escape/reopen clears the credential and acknowledgment; submission clears the input after React captures the request. Passwords never enter browser storage, returned action state, logs, or database records.
- Server verifies current account, then reserves a password-check attempt and uses an isolated Supabase Auth session to verify the existing password. The isolated session is locally signed out; the browser session is not replaced. Five attempts per 15 minutes per account supplement Auth rate limits.
- Supabase binds short-lived approval to customer, wallet, request key, cents, USD, test-card token, default preference, policy version and fresh password-authentication session. Each authentication session can authorize one logical deposit. A database trigger gates new funding sessions; retries and existing-session reconciliation retain one original credit. Existing historical payments remain recoverable.
- Authorization evidence is private and immutable apart from its one-time consumption link. Authentication-attempt history is private and append-only. No credentials or card numbers are stored in these records.
- Additive schema migrations `20260921180000`, `20260921181000`, `20260921200000`; separate enforcement migration `20260921201000` must run only after the matching app deploys.
- Verification before rollout: 350 application tests, TypeScript and targeted ESLint pass; 288 local wallet SQL assertions pass, including 31 authorization assertions. All 31 authorization checks also passed against the hosted schema in a rollback-only dry run. Four separate concurrent-request checks pass: same approval, same funding request, one ledger credit, and throttled password attempts. Local concurrency fixtures are retained as audit evidence; no existing demo data was deleted.
- Real-card launch still needs the selected processor's authentication, risk, dispute and refund integration. This account-password check is not cardholder verification or a compliance guarantee. References: [OWASP transaction authorization](https://cheatsheetseries.owasp.org/cheatsheets/Transaction_Authorization_Cheat_Sheet.html), [Supabase authentication claims](https://supabase.com/docs/guides/auth/jwt-fields), [Stripe 3-D Secure](https://docs.stripe.com/payments/3d-secure), [FTC charge disputes](https://consumer.ftc.gov/articles/using-credit-cards-and-disputing-charges).
- Live rollout: code checkpoint `1c8a1f3` built successfully at `https://zero-loss-h528vouq3-zero-loss.vercel.app`; the existing experiment alias points there. All four migrations are registered and `funding_reauth_required=true`. Main and production remain untouched.
- Live browser checks: blank submission prevented; one deliberately wrong password rejected with clear feedback and cleared input; Cancel, Escape, exact $1 amount, reload recovery and 390px mobile layout verified. Global counts stayed at 8 funding requests and 36 ledger rows, and the owner's balance stayed $194. No hosted money or entry records were added by these negative tests. One private authentication-attempt audit record is expected.
- Final positive UI check is awaiting the owner typing the correct password directly into the prepared $1 demo confirmation. Do not claim that real Auth-to-funding success has been verified until that succeeds and its one ledger credit is reconciled. No password was requested in chat.

## Thirty-second Undo Entry checkpoint — September 21

- Owner's positive password-confirmed $1 funding check is reconciled: one authorization, one succeeded $1 funding session, one $1 ledger credit; Playable Balance $195. No extra funding was added by this inspection.
- Approved workflow: reserve the entire 1–10-ticket submission for 30 server-timed seconds; Undo releases that whole reservation to the same Playable Balance, not to the card. Final entries and their outcomes/shares are created only after the deadline. This is the owner's previously approved accidental-entry policy, not a refund of an accepted entry or a deposit.
- `public.entry_requests` implements the canonical pre-Entry workflow (`validating`, `accepted`, `cancelled`, `rejected`). Snapshot fixture outcomes stay private; owner-readable audit events use `entry.requested`, `entry.accepted`, `entry.cancelled`, `entry.rejected`. Prior entries and outcomes remain immutable and untouched.
- Ledger-owned hold/release interface derives integer cents from immutable request terms. Reservation and release each have unique request identities, composite wallet ownership, and append-only history. Finalization replaces the hold with ordinary per-entry debits atomically; no new balance authority is introduced.
- Reserved slots count in ordinary availability. Existing repeatable TV/shoes/baby scenarios are preserved. Account/offer eligibility is checked again at acceptance; a changed restriction rejects/releases the reservation. Public legacy RPCs cannot bypass the window once enabled. Repeated requests for the same pending prize resume that request, even with a new page-generated key.
- Site-wide navy/cyan Undo notice survives navigation/reload by re-reading Supabase, supports batches and truthful network errors, and offers exact-result links after acceptance. Wallet history/notifications distinguish holds, hold releases, and final purchases. Browser timers are display-only and do not authorize value changes.
- Private server worker uses a non-overlapping job and pre-acquires customer locks before offering locks, skipping busy customers to avoid cross-customer lock inversions. [Supabase Cron](https://supabase.com/docs/guides/cron/quickstart) supports the five-second scheduler; server completion does not require an open browser.
- Two-phase rollout: schema `20260921210000` installed with `entry_undo_required=false`; enable migration `20260921212000` must follow the compatible Ready deployment and registers the private worker. Production/main remain out of scope.
- Verification so far: 359 application tests; TypeScript, changed-file ESLint and production build pass. 340 local database assertions, 52 hosted rollback-only assertions, and six independent simultaneous-request tests pass. The real 30-second local timing test rejects late Undo and proves one net charge. New local-only audit fixtures are retained; no existing records were deleted.
- This remains simulated funding/entries. Production draw lock integration, processor refunds/chargebacks, identity provider, support value approvals and real fulfillment remain release gates. Stable retry identity for an unknown result older than the recent-receipt display window remains a follow-up; do not claim all long-disconnected client recovery cases are solved.
- Live rollout: application checkpoint `ae8cfb1` is Ready at `https://zero-loss-1jjbut87x-zero-loss.vercel.app`, and the existing experiment alias was moved there. Both schema and enable migrations are registered; five-second private Cron job is active with successful runs. Main and production were untouched.
- Live UI checks: one private $1 Best Buy test entry confirmed after the window; one $1 submission was undone; a two-ticket $2 submission survived reload at 390px phone width, displayed 22 seconds remaining, and both tickets were undone. Phone page had no horizontal overflow; viewport override reset. This added **one** finalized demo ticket and used **$1** net; owner's balance is $194. No real payment, real reward or Crew share occurred.
- Hosted postflight: accepted request has one entry; cancelled requests have zero; all three holds net to zero after release; all requests retain requested/resolved audit events; no overdue requests or hold/entry-count mismatches. A small client follow-up prevents an old in-flight page read from suppressing restoration after navigation and avoids repeatedly resetting a submission key for already-seen terminal receipts.
- Final code checkpoint `3364dc8` is Ready at `https://zero-loss-edebn9ahy-zero-loss.vercel.app`; the experiment alias now points there. The build log confirms the exact commit and branch. Final application suite: **361 passing tests**. No production/main alias changes.
- Final live postflight includes two additional one-ticket requests: one accepted and one undone after navigating from the prize to Wallet. Across this checkpoint's five live requests, **two** were accepted and **three** cancelled (cancelled quantities 1, 2 and 1). Exactly **two new $1 demo entries** remain, for **$2 net simulated spending** and a final Playable Balance of **$193**. No extra funding, real payment, real reward or Crew share occurred. My Activity and the exact saved-entry detail link were verified.
- Final hosted reconciliation: both accepted requests have exactly one saved entry each; cancelled requests have none; all five hold/release pairs net to zero; each request has two audit events. Hold mismatches, entry-count mismatches and overdue requests are all **zero**. The five-second worker remains active and its latest runs succeeded. The phone viewport override was reset after verification.

## Durable recovery and support checkpoint — September 21

- Removed the two-minute receipt visibility cutoff. Undismissed completed requests are recoverable from Supabase across reload/navigation/disconnection; dismissal is an owner-checked, durable acknowledgment, not browser-only state.
- New compare-and-submit RPC atomically returns the original receipt to a stale client instead of reserving/debiting again. New intentional submissions name the last displayed completed request; concurrent tabs with that same prior request cannot create two next submissions. Every key, including a key redirected to an older receipt, remains bound to its original result. Interrupted client retries preserve exact terms.
- Additive recovery schema `20260921220000`; legacy enforcement `20260921223000` is enabled only after the compatible application is Ready. Existing entries, ledger, showcase scenarios and How It Works remain untouched.
- Replaced the Support placeholder with authenticated intake, transaction-linked case context, customer conversation/status, paginated history and a separately authorized private reviewer inbox. Wallet history has a direct Report a problem link for each transaction. Customer Notifications links to the exact case; no external email is sent.
- Support schema `20260921230000` stores private cases, append-only messages and private administrative access evidence. RLS/definer functions enforce customer isolation, exact Ledger ownership, replay safety and reply throttles. Review access does not allow refunds, balance changes, reward issuance or arbitrary Ledger reads. No actual reviewer grant has been made; the owner login choice is pending.
- Unknown message responses use the same exact request on retry. Staff cannot act as staff on their own case; customer follow-up reopens a resolved conversation. Messages and authorization evidence cannot be overwritten or deleted as cleanup.
- Pre-rollout evidence: 405 local database assertions passed; hosted rollback-only checks passed 30 entry-recovery and 35 Support assertions. Three separate concurrent/real-time recovery checks passed, including a 122-second disconnection after server acceptance and two simultaneous tabs. Local-only audit fixtures are retained. No hosted funds, tickets or cases have been added in this checkpoint yet.
- Final application validation before rollout: 400 tests pass across 61 files; TypeScript, changed-file ESLint and production build pass. Additive recovery and Support schemas are installed on the hosted development-test project; legacy recovery enforcement remains off until the matching application deployment is Ready.
- Live transaction click-through exposed a protected-column mismatch in the new intake page: filtering Ledger by `wallet_scope` requires a deliberately denied column grant. Repaired by using the server-authenticated wallet snapshot's scope and the existing owner/current-wallet RLS, reading only allowed fields. The write RPC independently checks owner/demo scope. Added both application and SQL regression checks without expanding Ledger permissions.
- Live rollout: checkpoint `75dd6e5`, then permitted-read correction `da6ee35`; final code is Ready at `https://zero-loss-oim72xmys-zero-loss.vercel.app`. Build logs name the exact commit/experiment branch. Only the existing experiment alias was moved there; main and production are untouched. All three migrations are registered; `entry_recovery_required=true`.
- Final checks: **402 application tests**, **407 local database assertions**, **37 hosted rollback-only Support assertions**, TypeScript, changed-file lint, and the Vercel production build pass. Earlier hosted recovery assertions and separate concurrency/timing checks remain valid; the follow-up changed only Support's permitted read.
- Live customer verification: an old Best Buy receipt reappeared after reload, opened its exact saved entry, and stayed dismissed after an owner acknowledgment. All five old confirmations were acknowledged without deleting their requests or entries. A wallet purchase's Report a problem shortcut prefilled the correct transaction; one clearly labeled internal demo case was submitted, a customer follow-up was added at 390px width, both survived reload, and Notifications reopened the exact case. No horizontal page overflow; viewport override reset afterward.
- Test evidence retained: Support case `e01eaf3d-7205-408a-a863-a779000615ac`, two append-only messages, linked to existing transaction `88878851-d966-4076-bcc0-1f430f755955`. No new hosted funds/tickets, refunds, rewards, Crew shares or external emails. Owner balance remains **$193** with **8 active entries**. All five old request hold pairs reconcile; entry-count/hold mismatches and overdue requests are zero; worker runs succeed.
- Private reviewer inbox logic was verified using rollback-only staff fixtures; **no live reviewer access has been granted** and live staff UI testing awaits the owner's login designation. This is not a claim of real-payment/refund/fulfillment readiness.

## Exact reward destinations and lifecycle access repair (September 21, 2026)

- Approved scope: repair Orders' ambiguous same-product gift-card link and prevent unavailable rewards from appearing usable. No layout redesign, deleted features, sample-data resets, financial adjustments or How It Works changes.
- Additive migration `20260921233000_reward_access_and_order_links` adds `reward_id` to the existing owner/current-wallet order projection. Orders uses the same exact reward link builder as My Activity; missing identifiers fail closed rather than reverting to product-only links.
- Claim retries now lock and reauthorize the owned current-wallet reward and check its authoritative lifecycle before any credential response. Expired, cancelled, redeemed, pending and failed issuance cannot return a credential through old or new request keys. Existing identity checks still gate new winner claims; valid repeat claims retain one claim and one credential.
- The reward screen displays lifecycle-specific feedback and keeps unusable records visible, with copy/present controls disabled. A stored reward never substitutes a product-derived sample when the credential read is missing or fails. Intentional illustrative cards without stored IDs retain their clearly marked samples. Valid saved rewards keep the existing design and sample disclosure.
- Pre-release verification: 427 application tests; TypeScript and changed-file ESLint; production build; 444 local wallet assertions plus 41 identity assertions. Hosted rollback-only regression: 37 assertions pass, including two purchases of the same product, exact order/reward correspondence, stale request replay, first claim after a terminal event, customer isolation and disabled-wallet rejection.
- Hosted preflight: 4 rewards, 4 reward events, 4 credentials, 2 orders, 49 Ledger entries; owner playable balance $193. Tests roll back and preserve the deployed demo's Undo Entry/recovery settings. No hosted test funds, tickets, rewards or orders were added.
- Release order: apply the backward-compatible projection/access migration, deploy the experiment branch, then verify the existing TV reward on desktop/mobile. Rollback should retain the hardened database functions; reverting the UI alone does not require data rollback.

## Sign-in continuity and notification destinations — September 21

- Approved small-batch scope: retain the selected destination through sign-in and make notification status/destinations accurate. No redesign, removed features, sample resets, policy changes, provider integration or How It Works changes.
- Extended the shared authentication destination allowlist to known account and Support pages. Exact reward IDs, entry selectors/filters, wallet views/transactions, Crew tabs/requests and Support cases survive login, signup and email verification. Foreign URLs, normalized traversal paths, duplicate selectors and unknown query fields are rejected. Destination pages still enforce ownership; a remembered URL does not grant access.
- Fixed Support's existing `next` link being silently discarded by the old entry/funding-only allowlist. No password-recovery behavior or email design was changed.
- Unavailable reward notifications use their authoritative lifecycle rather than the fact that the entry originally won. They retain a details link without promising a usable barcode; lifecycle-specific receipt IDs keep a new status distinct from an already-read ready notice. The older projection's generic “ready to claim” metadata is not reused for unavailable rewards. Closed purchase options do not invite another payment.
- Wallet notification links select/highlight the exact ledger row. An older linked transaction can be read separately from the existing latest-50 snapshot using only permitted columns, authenticated owner filtering and current-wallet RLS. Missing/foreign rows show feedback without substituting another transaction or changing the balance/history count. Crew requests now have exact anchors; Support notifications retain exact case links.
- Existing `customer_notification_reads` already supplies durable, idempotent owner-isolated receipts; no new table or hosted schema migration is necessary. Added 18 rollback-only SQL assertions covering receipt persistence/retries/isolation and an older transaction outside the latest 50. All 18 passed locally and on the hosted development-test project. Fixtures roll back; no hosted funds, entries, cases, invitations or rewards were created.
- Pre-publish checks: 521 application tests passed before the final transaction-anchor assertion; 462 local wallet database assertions passed; TypeScript, changed-file lint and production build passed. Signed-out local browser walkthrough preserved the TV reward across login → signup → login at desktop and 390px phone width. Full live password entry was not exercised; server-action and email-callback return behavior were verified by automated tests.

## Provider research (September 21, 2026)

- GiftCard Partners supports recipient delivery and provider-generated claim links, depending on product eligibility. Preserve one order identifier across retries; a timeout must not create a second order with a new identifier. [Orders](https://developers.giftcardpartners.com/docs/orders), [Delivery types](https://developers.giftcardpartners.com/docs/delivery-types).
- Fulfillment can be asynchronous. Treat callbacks as retryable signals and reconcile order status, not as permission to issue another reward. [Webhooks](https://developers.giftcardpartners.com/docs/webhooks).
- Stage credentials and its test-balance behavior need confirmation from the provider before actual stage orders. No provider orders were placed in this audit. [Sandbox](https://developers.giftcardpartners.com/docs/sandbox).
- Stripe illustrates why refunds need their own durable state: original payment-method destination, partial-refund limits, pending/failure states, and dispute interaction. This informs the proposed design; it does not set Zero Loss's refund entitlement policy. [Refund documentation](https://docs.stripe.com/refunds).
